config {
  type: "assertion",
  description: "Garante que os dados chegaram nas últimas 4 horas"
}

SELECT
  *
FROM
  ${ref("weather_cleaned")}
HAVING
  MAX(ingested_at) < TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 4 HOUR)